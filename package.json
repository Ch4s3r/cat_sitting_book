{
  "name": "cat-sitting-book",
  "version": "1.0.0",
  "private": true,
  "description": "A comprehensive guide to cat sitting",
  "author": "Ch4s3r",
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "linkcheck": "markdown-link-check docs/*.md --config .markdown-link-check.json && node scripts/check-assets.js",
    "test": "npm run build && npm run linkcheck"
  },
  "dependencies": {
    "@docusaurus/core": "3.0.1",
    "@docusaurus/plugin-pwa": "3.0.1",
    "@docusaurus/preset-classic": "3.0.1",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.0.1",
    "@docusaurus/types": "3.0.1",
    "glob": "^11.0.3",
    "markdown-link-check": "^3.13.7"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
